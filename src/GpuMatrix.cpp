#include <iostream>
#include <assert.h>

// Headers of file declaration
#include "GPUOperations.h"



template <typename T> class GpuMatrix {
private:
    /* data */
public:
    GpuMatrix(/* args */);
    ~GpuMatrix();
};

template <typename T>
GpuMatrix<T>::GpuMatrix(/* args */)
{
}

template <typename T>
GpuMatrix<T>::~GpuMatrix()
{
}